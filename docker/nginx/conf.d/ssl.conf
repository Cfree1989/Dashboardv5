# SSL/TLS Security Configuration
# This file contains additional SSL-specific security settings

# DH parameters for perfect forward secrecy (generate with: openssl dhparam -out dhparam.pem 2048)
# Uncomment and generate DH params for enhanced security:
# openssl dhparam -out /etc/ssl/certs/dhparam.pem 2048
# ssl_dhparam /etc/ssl/certs/dhparam.pem;

# Additional OCSP configuration if needed
# resolver 1.1.1.1 1.0.0.1 valid=300s;
# resolver_timeout 5s;

# HSTS (HTTP Strict Transport Security) - configured per-server in default.conf
# Additional security headers can be added here if needed
